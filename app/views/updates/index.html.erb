<li><%= link_to('New Manual Update', 'updates/new') %></li>

<h2>All Updates</h2>

<% @updates.each do |ups| %>
  <ul>
    <li><%= link_to(ups.title, ups) %></li>
    <li><%= link_to('Edit', "updates/#{ups.id}/edit") %>
    <li><%= link_to('Delete', @ups, :method => 'delete', :data => {:confirm => 'Are you sure?'}) %></li>
  </ul>
<% end %>